@Test
void testFeetToInchesConversion() {
    assertEquals(12.0, UnitConverter.feetToInches(1.0));
}

@Test
void testInchesToFeetConversion() {
    assertEquals(1.0, UnitConverter.inchesToFeet(12.0));
}

@Test
void testFeetAndInchesEquality() {
    assertTrue(QuantityMeasurementApp
            .checkFeetAndInchesEquality(1.0, 12.0));
}
